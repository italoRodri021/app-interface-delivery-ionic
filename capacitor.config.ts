import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'APP_INTERFACE_DELIVERY',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
